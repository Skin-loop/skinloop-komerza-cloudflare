{
  "name": "@komerza/skinloop-rust-cloudflare",
  "private": true,
  "version": "1.0.0",
  "description": "Standalone Cloudflare Worker bridge for Komerza orders and Skinloop Rust payments",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npm run db:migrate && wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply DB --remote",
    "test": "node --import tsx --test test/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "cloudflare": {
    "bindings": {
      "SKINLOOP_API_KEY": {
        "description": "Skinloop server API key with checkout:create and checkout:read scopes, restricted to Rust."
      },
      "KOMERZA_API_KEY": {
        "description": "Komerza server API key for the merchant store. This remains in the merchant's Cloudflare account."
      },
      "KOMERZA_STORE_ID": {
        "description": "Komerza store UUID that owns the orders handled by this Worker."
      },
      "SHOP_URL": {
        "description": "Customer-facing Komerza shop URL used by return and cancel pages."
      }
    }
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250906.0",
    "typescript": "^5.6.3",
    "tsx": "^4.21.0",
    "wrangler": "^4.0.0"
  }
}